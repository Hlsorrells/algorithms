// Math.min() expects each variable as separate param
// ... spread operator
    // allows an expression to be expanded in places where multiple elements/variables/arguments are expected

const arrMin = arr => Math.min(...arr);